import logging

logging.basicConfig(filename='diagnosis.log', level=logging.DEBUG)
# Use logging.debug(), logging.info(), logging.warning(), logging.error(), logging.critical() for different log levels